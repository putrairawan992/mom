const GET_BY_ID = `product/`
const CREATE = 'product';
const PRODUCT = 'product';
const CATEGORY = `${PRODUCT}/category`

export const PATH_PRODUCT = {
  CREATE: CREATE,
  GET_BY_ID: GET_BY_ID,
  CREATE: CREATE,
  PRODUCT: PRODUCT,
  CATEGORY: CATEGORY
}