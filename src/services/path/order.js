//MANAGE ORDER
const MANAGE_ORDER = 'manage-order';
const NEXT = `${MANAGE_ORDER}/next`;
const UNDO = `${MANAGE_ORDER}/undo`;
const CANCEL = `${MANAGE_ORDER}/cancel`;

export const PATH_ORDER = {
    MANAGE_ORDER: MANAGE_ORDER,
    NEXT: NEXT,
    UNDO: UNDO,
    CANCEL: CANCEL
}